# 
# docker-compose.yml — определяет сервисы для разработки и тестирования.

# docker-compose.yml — defines services for dev/test
# Services: PostgreSQL with init DB, Spring Boot app (optional)
# Expose ports: 5432, 8080
# Volume for DB persistence
